# .env.example for platform
# copy to .env and adjust values as needed

# HTTP port for agent runtime
PORT=8080

# development mode (using tsx over prebuilts)
PLATFORM_DEV=true

# workspace and trigger directories (override for local dev)
WORKSPACE_DIR=./working-dir
TRIGGERS_DIR=./working-dir/.triggers

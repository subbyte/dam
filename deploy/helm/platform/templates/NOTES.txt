{{ .Values.brand.name }} has been deployed!

Components:
  - PostgreSQL: {{ include "platform.postgres.fullname" . }}.{{ .Release.Namespace }}.svc:5432
{{- if .Values.keycloak.enabled }}
  - Keycloak:   {{ include "platform.keycloak.fullname" . }}.{{ .Release.Namespace }}.svc:{{ .Values.keycloak.port }}
    - Admin:    {{ include "platform.url.keycloak" . }} (user: {{ .Values.keycloak.admin.user }})
    - Realm:    {{ .Values.keycloak.realm }}
    {{- if .Values.keycloak.testUser.enabled }}
    - Test user: {{ .Values.keycloak.testUser.username }} / {{ .Values.keycloak.testUser.password }}
    {{- end }}
{{- end }}

Agent namespace: {{ .Values.agentNamespace }}

RBAC:
  Controller SA: {{ include "platform.controller.serviceAccountName" . }}
  API Server SA: {{ include "platform.apiserver.serviceAccountName" . }}

To verify:
  kubectl get pods -n {{ .Release.Namespace }}
  kubectl get cm -n {{ .Values.agentNamespace }} -l agent-platform.ai/type=agent-template

To access services:
  {{ include "platform.url.ui" . }}          (UI)
{{- if .Values.keycloak.enabled }}
  {{ include "platform.url.keycloak" . }}    (Keycloak admin)
{{- end }}

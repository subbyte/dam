# Code generated by controller-gen. DO NOT EDIT.
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    agent-platform.ai/crd-schema-generation: "1"
    controller-gen.kubebuilder.io/version: v0.21.0
    helm.sh/resource-policy: keep
  name: runs.agent-platform.ai
spec:
  group: agent-platform.ai
  names:
    kind: Run
    listKind: RunList
    plural: runs
    singular: run
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .spec.agentName
      name: Agent
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: Run is an ephemeral single-command executor derived from an Agent.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              RunSpec is an ephemeral executor derived from an Agent, backing the in-pod
              `dam-run` CLI: it materializes a throwaway sandbox pod (same image, config,
              and RWX workspace as the parent) that runs one command streamed over
              /api/exec. Unlike a Fork it runs as the parent Agent's own owner. The command
              argv is deliberately NOT stored here — it travels only over the exec
              WebSocket — so the executor pod is generic and no command bytes land in etcd.
              The api-server is the sole writer of the spec and deletes the Run when the
              streaming connection ends.
            properties:
              agentName:
                description: AgentName names the parent Agent this run derives from.
                type: string
            required:
            - agentName
            type: object
          status:
            description: |-
              RunStatus is the observed state of an executor run. The controller is the
              sole writer, via the status subresource.
            properties:
              error:
                description: RunError carries a structured failure reason on a failed
                  run.
                properties:
                  detail:
                    type: string
                  reason:
                    type: string
                required:
                - reason
                type: object
              phase:
                description: RunPhase is the lifecycle phase of an executor run.
                enum:
                - Pending
                - Ready
                - Failed
                - Completed
                type: string
              podIP:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}

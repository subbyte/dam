{{- if .Values.codeGuardianTemplate.enabled }}
{{- $home := .Values.codeGuardianTemplate.agentHome }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Values.codeGuardianTemplate.name }}
  namespace: {{ .Values.agentNamespace }}
  labels:
    {{- include "humr.labels" . | nindent 4 }}
    humr.ai/type: agent-template
data:
  spec.yaml: |
    version: humr.ai/v1
    image: "{{ .Values.codeGuardianTemplate.image.repository }}:{{ .Values.codeGuardianTemplate.image.tag | default .Chart.AppVersion }}"
    description: {{ .Values.codeGuardianTemplate.description | quote }}
    mounts:
      - path: {{ $home }}
        persist: true
        {{- with .Values.codeGuardianTemplate.homeMountSize }}
        size: {{ . | quote }}
        {{- end }}
      - path: /tmp
        persist: false
    init: |
      #!/bin/bash
      # Seed home from image on first boot
      if [ ! -f {{ $home }}/.initialized ]; then
        cp -rn /app/working-dir/. {{ $home }}/ 2>/dev/null || true
        touch {{ $home }}/.initialized
      fi
      mkdir -p {{ $home }}/work
    env:
      - name: PORT
        value: "8080"
      {{- with .Values.codeGuardianTemplate.githubRepo }}
      - name: GITHUB_REPO
        value: {{ . | quote }}
      {{- end }}
    resources:
      requests:
        cpu: {{ .Values.codeGuardianTemplate.resources.requests.cpu | quote }}
        memory: {{ .Values.codeGuardianTemplate.resources.requests.memory | quote }}
      limits:
        cpu: {{ .Values.codeGuardianTemplate.resources.limits.cpu | quote }}
        memory: {{ .Values.codeGuardianTemplate.resources.limits.memory | quote }}
    securityContext:
      readOnlyRootFilesystem: false
    skillPaths:
      - {{ $home }}/.claude/skills/
    {{- with .Values.codeGuardianTemplate.skillSources }}
    skillSources:
      {{- range . }}
      - name: {{ .name | quote }}
        gitUrl: {{ .gitUrl | quote }}
      {{- end }}
    {{- end }}
{{- end }}

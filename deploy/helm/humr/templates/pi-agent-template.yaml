{{- if .Values.piAgentTemplate.enabled }}
{{- $home := .Values.piAgentTemplate.agentHome }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Values.piAgentTemplate.name }}
  namespace: {{ .Values.agentNamespace }}
  labels:
    {{- include "humr.labels" . | nindent 4 }}
    humr.ai/type: agent-template
data:
  spec.yaml: |
    version: humr.ai/v1
    image: "{{ .Values.piAgentTemplate.image.repository }}:{{ .Values.piAgentTemplate.image.tag | default .Chart.AppVersion }}"
    description: {{ .Values.piAgentTemplate.description | quote }}
    mounts:
      - path: {{ $home }}
        persist: true
        {{- with .Values.piAgentTemplate.homeMountSize }}
        size: {{ . | quote }}
        {{- end }}
      - path: /tmp
        persist: false
    init: |
      #!/bin/bash
      # Seed home from image on first boot
      if [ ! -f {{ $home }}/.initialized ]; then
        cp -rn /app/working-dir/. {{ $home }}/ 2>/dev/null || true
        touch {{ $home }}/.initialized
      fi
      mkdir -p {{ $home }}/work
    env:
      - name: PORT
        value: "8080"
    resources:
      requests:
        cpu: {{ .Values.piAgentTemplate.resources.requests.cpu | quote }}
        memory: {{ .Values.piAgentTemplate.resources.requests.memory | quote }}
      limits:
        cpu: {{ .Values.piAgentTemplate.resources.limits.cpu | quote }}
        memory: {{ .Values.piAgentTemplate.resources.limits.memory | quote }}
    securityContext:
      readOnlyRootFilesystem: false
    skillPaths:
      - {{ $home }}/.pi/agent/skills/
    {{- with .Values.piAgentTemplate.skillSources }}
    skillSources:
      {{- range . }}
      - name: {{ .name | quote }}
        gitUrl: {{ .gitUrl | quote }}
      {{- end }}
    {{- end }}
{{- end }}

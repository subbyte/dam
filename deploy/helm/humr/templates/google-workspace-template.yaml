{{- if .Values.googleWorkspaceTemplate.enabled }}
{{- $home := .Values.googleWorkspaceTemplate.agentHome }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Values.googleWorkspaceTemplate.name }}
  namespace: {{ .Values.agentNamespace }}
  labels:
    {{- include "humr.labels" . | nindent 4 }}
    humr.ai/type: agent-template
data:
  spec.yaml: |
    version: humr.ai/v1
    image: "{{ .Values.googleWorkspaceTemplate.image.repository }}:{{ .Values.googleWorkspaceTemplate.image.tag | default .Chart.AppVersion }}"
    description: {{ .Values.googleWorkspaceTemplate.description | quote }}
    mounts:
      - path: {{ $home }}
        persist: true
        {{- with .Values.googleWorkspaceTemplate.homeMountSize }}
        size: {{ . | quote }}
        {{- end }}
      - path: /tmp
        persist: false
    init: |
      #!/bin/bash
      # Seed home from image on first boot
      if [ ! -f {{ $home }}/.initialized ]; then
        cp -rn /app/working-dir/. {{ $home }}/ 2>/dev/null || true
        touch {{ $home }}/.initialized
      fi
      mkdir -p {{ $home }}/work
    env:
      - name: PORT
        value: "8080"
    resources:
      requests:
        cpu: {{ .Values.googleWorkspaceTemplate.resources.requests.cpu | quote }}
        memory: {{ .Values.googleWorkspaceTemplate.resources.requests.memory | quote }}
      limits:
        cpu: {{ .Values.googleWorkspaceTemplate.resources.limits.cpu | quote }}
        memory: {{ .Values.googleWorkspaceTemplate.resources.limits.memory | quote }}
    securityContext:
      readOnlyRootFilesystem: false
    skillPaths:
      - {{ $home }}/.claude/skills/
    {{- with .Values.googleWorkspaceTemplate.skillSources }}
    skillSources:
      {{- range . }}
      - name: {{ .name | quote }}
        gitUrl: {{ .gitUrl | quote }}
      {{- end }}
    {{- end }}
{{- end }}
